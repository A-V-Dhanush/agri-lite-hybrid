# =============================================================================
# AgriLite-Hybrid - Raspberry Pi Requirements
# Minimal dependencies for edge deployment
# =============================================================================

# TensorFlow Lite for inference (lighter than full TensorFlow)
tflite-runtime==2.14.0

# Image processing
Pillow>=9.0.0
opencv-python-headless>=4.5.0

# Web server (optional, for local access)
Flask>=3.0.0
flask-cors>=4.0.0

# Camera support (Pi camera)
picamera2>=0.3.0; platform_machine == "aarch64"

# Utilities
numpy>=1.23.0

# JSON handling (included in Python stdlib)
# json is built-in

# Note: Install with:
# pip3 install -r requirements_pi.txt
